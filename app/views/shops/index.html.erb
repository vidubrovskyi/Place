<%#= select_tag "filter", options_for_select(["bar", "pizza"]) %>
<%#= filter("bar") %>
<%#= link_to_if @shop.place_type == name, 'Poster', :controller => 'users',
               :action => 'show', :id => message.user %>

<% @shops.each do |shop| %>
  <div class="block mb-2">
    <%= check_box_tag :place_type%>
    <%= shop.place_type %>
  </div>
<% end %>
_________________
<br>

<%= link_to "Add new place", new_shop_path %>

<% @shops.each do |shop| %>
  <p><%= shop.name %></p>
  <p><%= shop.address %></p>
  <p><%= shop.description %></p>
  <p><%= shop.contact %></p>
  <p><%= shop.place_type %></p>
  <% if shop.images.attached? %>
    <% shop.images.each do |image| %>
      <p><%= image_tag image, width: "100px" %></p>
    <% end %>
  <% end %>
  <p><%= link_to "Show", shop_path(shop) %></p>
  <p><%= link_to "Edit", edit_shop_path(shop) %></p>
  <p><%= link_to "Delete", shop_path(shop), data: { "turbo-method": :delete, turbo_confirm: "You sure?" } %></p>
<% end %>