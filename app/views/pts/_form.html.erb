<%= form_with(model: pt, class: "contents") do |form| %>

  <%= render 'static_page/errors', object: pt %>

  <div class="my-5">
    <%= form.label :pt %>
    <%= form.text_field :pt, class: "form-control block shadow rounded-md border border-gray-200 outline-none px-3 py-2 mt-2 w-full" %>
  </div>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>

<% end %>
